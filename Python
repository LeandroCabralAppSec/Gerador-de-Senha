import random, string

tamanho = int(input('Qual o tamanho da sua senha? Digite um número: '))

chars = string.ascii_letters + string.digits + '&%$#@!/\*-{[}]?=()'

rnd = random.SystemRandom()

print(''.join(rnd.choice(chars) for i in range(tamanho)))
